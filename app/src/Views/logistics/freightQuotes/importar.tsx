export {};

/*
import { ReactNode } from 'react';
import { Upload, TextBox, ViewUpload } from '../../../Utils/Controls';
import { EventArgs } from '../../../Utils/EventArgs';

export class ViewImportar extends ViewUpload {

    state = {
        open: false,

        files: []
    }

    public Close = () => this.setState({open: false});

    public Cancel = () => undefined;
   
    public Confirm = () => this.state;
 
    public render(): ReactNode {

        return (
            <Upload Open={this.state.open} Title="Importar" Close={this.Close} Cancel={this.Cancel} Confirm={this.Confirm}>
                <input type="file" name="fileUploaded" multiple />
            </Upload>
        );

    }

}
*/